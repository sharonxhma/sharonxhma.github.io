/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // basePath: "/sharonxhma.github.io",
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;
